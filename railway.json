{
  "$schema": "https://railway.app/railway.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "startCommand": "npm start",
    "env": {
      "NODE_ENV": "production"
    }
  },
  "deploy": {
    "numReplicas": 1
  }
}